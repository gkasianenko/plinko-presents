{
  "name": "plinko",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:click": "vite build --mode click",
    "build:auto": "vite build --mode auto",
    "build:both": "npm run build:click && npm run build:auto",
    "deploy:both": "npm run build:both && firebase deploy"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "vite-plugin-singlefile": "^2.3.0"
  },
  "dependencies": {
    "matter-js": "^0.20.0"
  }
}
